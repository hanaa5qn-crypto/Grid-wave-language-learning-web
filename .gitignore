node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
# Secret env files that don't start with ".env" (e.g. Azure TTS keys)
*-tts.env
azure-tts.env

# Firebase CLI cache (deploy logs / hosting cache)
.firebase/

# Local npm cache (sandboxed installs)
.npm-cache/

# Local tooling / agent artifacts (not part of the app)
.antigravity/
.playwright-mcp/
.vscode/
.vercel

# Ignore all marketing assets and folders
marketing/

# Service-account credentials (Vertex AI) — never commit
.secrets/
